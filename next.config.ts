import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The contents of this function need to be commented out in order to run the dev environment
};

export default nextConfig;
